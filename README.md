# interpreterforlinux
Its Just An Interpreter For Linux

goto `files` and put your code in `main.py`

now go back by:
```bash
cd ..
```

enter this command:
```bash
make compile
```
if it shows:
```bash
make: command not found
```
then install `'mtools'`
